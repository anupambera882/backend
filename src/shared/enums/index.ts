export enum GENDER {
  MALE = 'Male',
  FEMALE = 'Female',
}

export enum ROLE {
  PATIENT = 'Patient',
  DOCTOR = 'Doctor',
  ADMIN = 'Admin',
}

export enum STATUS {
  PENDING = 'Pending',
  ACCEPTED = 'Accepted',
  REJECTED = 'Rejected',
}
